server:
  port: 8082

spring:
  application:
    name: variant-c-spring
  
  datasource:
    url: jdbc:postgresql://localhost:5432/rest_api_perf
    username: perfuser
    password: perfpass
    driver-class-name: org.postgresql.Driver
    
    # HikariCP Configuration
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      pool-name: HikariPool-Spring
  
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        # Disable L2 cache
        cache:
          use_second_level_cache: false
          use_query_cache: false
        # Performance settings
        jdbc:
          batch_size: 20
          fetch_size: 50
        order_inserts: true
        order_updates: true
        # Logging
        show_sql: false
        format_sql: false
    show-sql: false

# Actuator & Prometheus
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
      base-path: /actuator
  endpoint:
    health:
      show-details: always
    prometheus:
      enabled: true
  metrics:
    export:
      prometheus:
        enabled: true
    tags:
      application: ${spring.application.name}
      variant: spring-restcontroller

# Logging
logging:
  level:
    root: INFO
    com.example: INFO
    org.hibernate: WARN
    com.zaxxer.hikari: INFO
